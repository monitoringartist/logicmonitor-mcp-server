# LogicMonitor Configuration (REQUIRED)
LM_COMPANY=mycompany
LM_BEARER_TOKEN=your-bearer-token-here

# MCP Server Configuration
MCP_TRANSPORT=sse
MCP_PORT=3000
MCP_DEBUG=false
MCP_LOG_FORMAT=json
MCP_LOG_LEVEL=info
MCP_READ_ONLY=true
MCP_DISABLE_SEARCH=false

# OAuth Configuration (optional, for HTTP/SSE modes)
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_CALLBACK_URL=http://localhost:3000/auth/callback

# Session Configuration (required for HTTP/SSE modes)
SESSION_SECRET=change-me-in-production-use-long-random-string

