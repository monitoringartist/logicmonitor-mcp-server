# LogicMonitor MCP Server Configuration

# Server Configuration
PORT=3000
NODE_ENV=development
BASE_URL=http://localhost:3000

# Session Configuration
SESSION_SECRET=your-session-secret-here-change-me

# GitHub OAuth Configuration
# Create a GitHub OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-client-id-here
GITHUB_CLIENT_SECRET=your-github-client-secret-here

# OAuth Callback URL (must match what you configured in GitHub)
CALLBACK_URL=http://localhost:3000/auth/github/callback

# CORS Configuration (comma-separated list of allowed origins)
# Use * for development, specific domains for production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Transport Mode (for multi-transport server)
# Options: both | http-only | sse-only
# - both: Enable both HTTP and SSE transports (default)
# - http-only: Only HTTP streamable transport
# - sse-only: Only SSE transport
TRANSPORT_MODE=both

# Logging Configuration
# Log Level: debug | info | warn | error
# - debug: All messages (most verbose)
# - info: Informational messages and above (default)
# - warn: Warnings and errors only
# - error: Errors only (least verbose)
LOG_LEVEL=info

# Log Format: json | chatty
# - json: Machine-friendly JSON format (default, no emoticons)
# - chatty: Human-friendly format with colors and emoticons
LOG_FORMAT=json

# LogicMonitor API Configuration
# Your LogicMonitor company/account name (subdomain)
# Example: if your portal is "mycompany.logicmonitor.com", use "mycompany"
LM_COMPANY=your-company-name

# LogicMonitor API Bearer Token (Default)
# Generate a bearer token in LogicMonitor: Settings > Users & Roles > Bearer Tokens
# This is the default token used for all API requests
# Can be overridden per-request using the X-LM-BEARER-TOKEN header
LM_BEARER_TOKEN=your-bearer-token-here

# Tool Filtering Configuration
# ONLY_READONLY_TOOLS: When set to true, only exposes read-only tools (tools with readOnlyHint: true)
# This is useful for environments where you want to prevent any modifications to LogicMonitor
# Options: true | false
# - true: Only expose read-only tools (list, get, search operations) (default - safer)
# - false: Expose all tools including write operations (create, update, delete)
ONLY_READONLY_TOOLS=true
